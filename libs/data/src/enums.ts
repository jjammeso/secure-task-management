export enum Role{
    OWNER = 'owner',
    ADMIN = 'admin',
    VIEWER = 'viewer'
}

export enum TaskStatus{
    TODO = 'todo',
    IN_PROGRESS = 'in_progress',
    DONE = 'done'
}

export enum TaskCategory {
    WORK = 'work',
    PERSONAL = 'personal',
    URGENT = 'urgent'
}